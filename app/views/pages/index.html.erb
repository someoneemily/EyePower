<h1>Listing Pages</h1>

<table>
  <tr>
    <th>Title</th>
    
    <th></th>
  </tr>
 
  <% @pages.each do |page| %>
    <tr>
      <td><%= page.title %></td>
      
      <td><%= link_to 'Show', page_path(page) %></td>
    </tr>
  <% end %>
</table>
<%= link_to 'New', new_page_path %>
